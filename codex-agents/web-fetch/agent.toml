# Agent definition for spawning a Codex run.

prompt = "prompt.txt"
workdir = "."

# Enable outbound network access within the workspace-write sandbox.
codex_args = [
  "--sandbox", "workspace-write",
  "-c", "sandbox_workspace_write.network_access=true"
]

# Optional: profile = "web-fetch"
